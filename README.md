# AI_PR_SUMMARIZER
When a developer opens a Pull Request (PR) on GitHub, your Python script will:  Fetch the "diff" (the code changes).  Send that code to an LLM (like Gemini or OpenAI).  Post a concise summary as a comment on the PR automatically.
