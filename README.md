# AI_PR_SUMMARIZER
When a developer opens a Pull Request (PR) on GitHub, your Python script will:  1.Fetch the "diff" (the code changes).  
2.Send that code to an LLM (like Gemini or OpenAI).  
3.Post a concise summary as a comment on the PR automatically.
